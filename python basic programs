num1,num2 = [int(i) for i in input("Enter the intervals (seperatered by commas) to print prime numbers bw them:").split(',')]
for i in range(num1,num2+1):
    if i>1:
        for j in range(2,i):
            if i%j==0:
                break
        else:
            print(i)
        
